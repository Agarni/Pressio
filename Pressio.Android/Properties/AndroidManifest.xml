<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:installLocation="auto">
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
	<uses-permission android:name="android.permission.health.READ_BLOOD_PRESSURE" />
	<uses-permission android:name="android.permission.health.WRITE_BLOOD_PRESSURE" />
	<uses-permission android:name="android.permission.health.READ_HEART_RATE" />
	<uses-permission android:name="android.permission.health.WRITE_HEART_RATE" />
	<application android:label="Pressio" android:icon="@drawable/Icon">
		<receiver android:name=".NotificationReceiver" android:exported="false" />
		<!-- Activity de autorização do Health Connect (abre a tela de permissões do app). -->
		<activity android:name="androidx.health.connect.client.PermissionActivity" android:exported="false" />
		<!-- Ações de privacidade do Health Connect. -->
		<provider android:name="androidx.core.content.FileProvider" android:authorities="${applicationId}.androidx-startup" android:exported="false" android:grantUriPermissions="true" />
	</application>
</manifest>
